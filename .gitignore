# ── 보안: 절대 커밋하지 않는 파일 ──
.env
.env.*
*.pem
*.key
*.cert
*.p12
credentials.json
secrets.json
token.json
*.secret

# API 키 / 토큰이 포함될 수 있는 설정
config.local.*
*.local.json
.netlify/
.vercel/

# ── OS / IDE ──
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~

# ── Node.js ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
yarn.lock
pnpm-lock.yaml

# ── 빌드 출력 ──
dist/
build/
.cache/
.parcel-cache/
*.tsbuildinfo

# ── Phaser / 게임 에셋 (대용량, 별도 관리) ──
# 원본 스프라이트는 용량이 크므로 assets/sprites/ 내
# 개별 PNG/JSON은 Git LFS 또는 별도 CDN으로 관리
# 아래는 임시/가공 에셋
assets/raw/
assets/temp/
*.atlas.tmp

# ── 테스트 / 커버리지 ──
coverage/
.nyc_output/

# ── 로그 ──
*.log
logs/

# ── 도구 ──
.playwright-cli/
